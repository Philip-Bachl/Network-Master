[package]
name = "frontend"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.49.0", default-features = false, features = ["macros"] }
yew = { version = "0.22.0", features = ["csr"] }
